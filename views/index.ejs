<% include partials/header %>

<div class="container my-md-5 text-light">
    <div class="row">
        <nav class="navbar navbar-dark bg-dark d-md-none text-center sticky-top secondary-nav">
            <button class="btn btn-secondary mx-auto" id="filter-toggle-button" type="button" data-toggle="collapse" data-target="#filter-controls" aria-controls="filter-controls" aria-expanded="false" aria-label="Toggle search filters">
              Show Search Filters
            </button>
        </nav>
        <div class="col-12 col-md-3 collapse d-md-block p-0 pr-md-2 sticky-top filter-controls" id="filter-controls">
            <div class="bg-dark p-2 rounded">
                <% if (keyword !== "" || queriedTags.length > 0) { %>
                    <h2><%= entries.length %> Results</h2>
                <% } else { %>
                    <h2>Past Entries</h2>
                <% } %>
                <form action="<%= "/entries/search" + (queriedTags.length > 0 ? "?tags=" + encodeURIComponent(queriedTags) : "") %>" method="POST">
                    <div class="input-group">
                        <input class="form-control" type="text" value="<%= keyword %>" name="searchterm" placeholder="Search by phrase..." aria-label="Search by phrase" aria-describedby="search-button">
                        <div class="input-group-append">
                            <button class="btn btn-primary" action="submit" id="search-button">Search</button>
                        </div>
                    </div>
                </form>

                <% if (keyword !== "") { %>
                    <p class="mt-2">Filtering by phrase: "<%= keyword %>"</p>
                    <a class="btn btn-outline-light mb-1" href="<%= "/entries?tags=" + encodeURIComponent(queriedTags) %>">Clear keyword</a><br>
                <% } %>

                <% if (queriedTags.length > 0) { %>
                    <p class="mt-2">Filtering by tags:
                        <% queriedTags.forEach(function(tag) { %>
                            <a class="badge badge-light p-1" href="<%= "/entries?tags=" + encodeURIComponent(queriedTags.filter(qTag => qTag != tag)) + "&keyword=" + keyword %>"><%= tag %></a>
                        <% }); %>
                    </p>
                <% } %>

                <% if (keyword !== "" || queriedTags.length > 0) { %>
                    <a class="btn btn-outline-light mb-1" href="/entries">Clear all search terms</a><br>
                <% } %>


                <h2 class="mt-5">Tags</h2>
                <% tags.forEach(function(tag) { %>
                    <a class="badge badge-light p-1 my-1 mr-1" href="<%= "/entries?tags=" + (queriedTags.length > 0 ? encodeURIComponent(queriedTags) + "," : "") + encodeURIComponent(tag.name) + "&keyword=" + keyword %>"><%= tag.name %> (<%= tag.entryCount %>)</a>
                <% }) %>
                <br>
                <a href="/entries/new" class="mt-5 btn btn-primary">Create new entry</a>
            </div>
        </div>
        
        <div class="col-12 col-md-9 my-5 my-md-0">
            <% if (entries.length === 0) { %>
                <p>No entries found.</p>
            <% } else { %>
                <% entries.forEach(function(entry) { %>
                    <div class="mb-5">
                        <a href="/entries/<%= entry._id %>">
                            <% const date = new Date(entry.dateCreated) %>
                            <% var months = ["January", "February", "March", "April", "May", "June", "July", "August", "September", "October", "November", "December"]; %>
                            <p class="mb-1"><%= `${months[date.getMonth()]} ${date.getDate()}, ${date.getFullYear()}` %></p>
                            <% entry.tags.forEach(function(tag) { %>
                                <a class="badge badge-light p-1 mr-1" href="<%= "/entries?tags=" + (queriedTags.length > 0 ? encodeURIComponent(queriedTags) + "," : "") + encodeURIComponent(tag) + "&keyword=" + keyword %>"><%= tag %></a>
                            <% }) %>
                            <p class="my-2"><%= entry.body.substring(0,500) %>...</p>
                        </a>
                        <a href="/entries/<%= entry._id %>" class="btn btn-outline-light" title="View">view</a>
                        <a href="/entries/<%= entry._id %>/edit" class="btn btn-outline-light" title="Edit">edit</a>
                        <form action="/entries/<%= entry._id %>?_method=DELETE" method="POST" class="d-inline">
                            <button action="submit" class="btn btn-outline-light" title="Delete">delete</button>
                        </form>
                    </div>
                <% }); %>
            <% } %>
        </div>
    </div>
</div>

<script>
    $('#filter-controls').on('show.bs.collapse', function() {
        $('#filter-toggle-button').html("Hide Search Filters");
    });

    $('#filter-controls').on('hide.bs.collapse', function() {
        $('#filter-toggle-button').html("Show Search Filters");
    });
</script>